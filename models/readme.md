File 'lgbm_model.pickle' is example of the model that is obtained during the training.  
Production model is always trained on freshly scraped data and mounted to the container with
API via docker volumes.